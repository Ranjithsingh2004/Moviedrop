node_modules/
.vercel
.DS_Store
*.log
convex/_generated/
.env.local

# Test-run screenshots (suites write these to the repo root).
# Anchored to the root so a real asset named the same way is unaffected.
/shot-*.png
/m-*.png
